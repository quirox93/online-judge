---
import CardForm from "@/components/CardForm";
import Layout from "@/layouts/Layout.astro";
export const prerender = true;

let cardNumber = Astro.url.searchParams.get('card_number') || '';


---

<Layout title="Prices" slug="home">
  <CardForm cardNumber={cardNumber} client:only />
</Layout>
